

<%= content_tag :h1, "List of Batches" %>


<div class="text-end mb-3">
  <%= link_to new_batch_path, class: 'btn btn-primary' do %>
    New Batch
  <% end %>
</div>


<div class="row mb-2">
  <div class="col-1">
    <%= link_to "18-65", batches_path(search: "regular"), class: "btn btn-outline-primary"  %>
  </div>
  <div class="col-1">
    <%= link_to "Overage", batches_path(search: "overage"), class: "btn btn-outline-warning"  %>
  </div>
  <div class="col-1">
    <%= link_to "View All", batches_path, class: "btn btn-outline-success"  %>
  </div>
</div>
   


<%== pagy_bootstrap_nav(@pagy) if @pagy.pages > 1 %>

<div class="table-responsive">
  <table class="table table-striped table-bordered table-hover">
    <thead>
      <tr>

      <th>ID</th>
        <th>Coop Member</th>
        <th>Age</th>
        <th>Group Remit</th>
        <th>Effectivity Date</th>
        <th>Expiry Date</th>
        <th>Active</th>
        <th>Coop Sf Amount</th>
        <th>Agent Sf Amount</th>
        <th></th>

      </tr>
    </thead>

    <tbody>
      <% @batches.each do |batch| %>
        <tr>

        <td><%= batch.id %></td>
        <td><%= batch.coop_member %></td>
        <td><%= batch.age %></td>
        <td><%= batch.group_remit %></td>
        <td><%= batch.effectivity_date %></td>
        <td><%= batch.expiry_date %></td>
        <td><%= batch.active %></td>
        <td><%= batch.coop_sf_amount %></td>
        <td><%= batch.agent_sf_amount %></td>
    
          <td>
            <%= link_to 'View', batch, class: 'btn' %>
            <%= link_to 'Edit', edit_batch_path(batch), class: 'btn' %>
            <%= link_to 'Delete', batch, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn' %>
          </td>

        </tr>
      <% end %>
    </tbody>
  </table>
</div>
