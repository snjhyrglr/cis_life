
<%= render @agreement %>

<div>
  <%= link_to "Edit", edit_agreement_path(@agreement), class: 'btn btn-primary' %> |
  <%= link_to "Back", agreements_path, class: 'btn btn-primary' %>

  <%= button_to "Delete", @agreement, method: :delete, class: 'btn btn-danger' %>
</div>
