
<%= render @agreement_benefit %>

<div>
  <%= link_to "Edit", edit_agreement_benefit_path(@agreement_benefit), class: 'btn btn-primary' %> |
  <%= link_to "Back", agreement_benefits_path, class: 'btn btn-primary' %>

  <%= button_to "Delete", @agreement_benefit, method: :delete, class: 'btn btn-danger' %>
</div>
