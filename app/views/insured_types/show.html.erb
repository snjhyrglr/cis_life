
<%= render @insured_type %>

<div>
  <%= link_to "Edit", edit_insured_type_path(@insured_type), class: 'btn btn-primary' %> |
  <%= link_to "Back", insured_types_path, class: 'btn btn-primary' %>

  <%= button_to "Delete", @insured_type, method: :delete, class: 'btn btn-danger' %>
</div>
